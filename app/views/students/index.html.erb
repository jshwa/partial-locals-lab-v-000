
<%= form_for @student, search_path do |t|%>
  <%= t.label :Search %>
  <%= t.input :name%>
  <%= t.submit %>
<% end %>

<% if @student %>
  <%= render partial: 'student', locals: {student: @student} %>
<% end %>
