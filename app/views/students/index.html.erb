
<%= form_for @student, url:search_path do |t|%>
  <%= t.label :Search %>
  <%= t.text_field :name%>
  <%= t.submit %>
<% end %>

<% if @student %>
  <%= render partial: 'student', locals: {student: @student} %>
<% end %>
