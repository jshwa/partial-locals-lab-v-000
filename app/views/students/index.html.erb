
<%= form_for @student, url:search_path do |t|%>
  <%= t.label :Search %>
  <%= t.text_field :name%>
  <%= t.submit %>
<% end %>

<% if @found_students %>
  <%= @found_student.each do |found_student| %>
    <%= render partial: 'student', locals: {student: @found_student} %>
  <% end %>
<% end %>
